{% extends 'base.html.twig' %}

{% block body %}
<div>
    {{ form_start(form) }}
        {{ form_errors(form) }}

        {{ form_row(form.title) }}
        {{ form_row(form.description) }}

        {{ form_row(form.save) }}
    {{ form_end(form) }}
</div>
{% endblock %}
